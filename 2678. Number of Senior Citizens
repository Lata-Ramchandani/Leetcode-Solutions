class Solution:
    def countSeniors(self, details: List[str]) -> int:
        count=0
        for i in range(len(details)):
            for j in details[i]:
                age=int(details[i][11:13:1])
            if age > 60:
                count+=1
        return count
